# Only for genes that are CDS, create all SNVs tables in KEGG_GENE_SCORES_P
# make sure to write columns:
#   (index), KeggId, NT_index (same for ~3 rows), AA_index (same for ~9 rows (3 mutations per nt)),
#   Variant (G347H), Ref (a), Alt (t)